{
  "name": "node-github-actions",
  "version": "1.0.0",
  "description": "Simple Node.js app with GitHub Actions",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"No tests yet\" && exit 0",
    "lint": "echo \"No linter configured\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
